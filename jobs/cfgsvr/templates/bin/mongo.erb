#!/usr/bin/env bash

/var/vcap/packages/mongodb/bin/mongo \
  --host <%= link('config-server').instances.map{|i| "#{i.address}:#{p('port')}"}.join(',') %> \
  --ssl --sslPEMKeyFile /var/vcap/jobs/cfgsvr/ssl/mongodb.pem \
  --sslCAFile /var/vcap/jobs/cfgsvr/ssl/CA.crt \
  --authenticationDatabase '$external' \
  --authenticationMechanism MONGODB-X509 "$@"