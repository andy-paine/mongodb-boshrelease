processes:
  - name: <%= p('job_name') %>
    executable: /var/vcap/packages/mongodb/bin/mongod
    persistent_disk: true
    ephemeral_disk: true
    args:
    - --config
    - /var/vcap/jobs/<%= p('job_name') %>/config/mongodb.conf